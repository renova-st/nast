export default {
  props: {
    value: { type: Number, default: 0, },
    loading: { type: Boolean, default: false, },
    close: { type: Function, default: () => {}, },
  },
  
  computed: {
    props() {
      return {
      }
    },
    events() {
      return {
      }
    },
    functions() {
      return {
      }
    },
  },
}
