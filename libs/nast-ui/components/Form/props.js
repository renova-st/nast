/**
 *
 */
export default {
  name: 'NForm',
  props: {
    submit: { type: Function, default: (event) => {}, },
  },

  computed: {
    props() {
      return {
      }
    },
    events() {
      return {
      }
    },
    functions() {
      return {
      }
    },
  },
}
